# spring-boot-data-crudrepository

This Spring Boot project implements full CRUD functionality using Spring Data JPA (CrudRepository) and Hibernate for MySQL persistence. It leverages Lombok annotations to ensure a clean, boilerplate-free codebase.
